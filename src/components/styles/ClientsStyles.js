import styled from "styled-components";

const ClientsStyles = styled.ul`
  background-color: transparent;
  list-style: none;
  padding: 0;
  li {
    margin: 0 0 1em 0;
  }
`;

export default ClientsStyles;
